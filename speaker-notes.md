# Заметки спикера

Рабочий документ ведущего: раскадровка по блокам, ключевые формулировки,
действия у клавиатуры, вопросы аудитории и запасные варианты. Держать открытым
на втором экране.

**Точные промпты — в `PROMPTS.md`.** Здесь указано, что говорить и что делать;
там — что вводить.

**Формат:** занятие для своих. Тайминг — ориентир, приоритет у практики,
выход за два часа допустим. Указанные интервалы даны от начала занятия.

---

## Перед началом (за 30 минут до эфира)

- [ ] Терминал: крупный шрифт (18+), контрастная тема, история очищена.
- [ ] Пустой репозиторий на GitHub создан, Actions включены, локальный клон готов.
- [ ] `claude` запускается в каталоге клона; `/doctor` без замечаний.
- [ ] `echo $ANTHROPIC_API_KEY` — понимать, какой счёт расходуется.
- [ ] GitHub MCP авторизован: `/mcp` показывает подключение.
- [ ] `docker info` отвечает; образы `postgres`, `redis`, `dunglas/frankenphp`
      и `php:8.4-cli` предварительно загружены; порты 8000, 8001, 5432, 6379 свободны.
- [ ] `jq` и `gitleaks` установлены (нужны для hooks).
- [ ] Эталонная конфигурация `reference-config/` под рукой — источник запасных файлов.
- [ ] Эталонная ветка с результатом полного прогона доступна.
- [ ] В чат закреплено: ссылка на материалы и требования к участникам.
- [ ] Открыт файл «парковки» для вопросов, уводящих в сторону.

**Правила эфира (проговорить в начале):** вопросы — в чат в любой момент;
после практик ставим «+»; всё, что не успеем разобрать, — в парковку и в финал.

---

## Блок 0 · 0:00–0:06 · Что построим

**Открывающий тезис (дословно):**
> «Claude Code — это не чат, куда вставляют куски кода. Это агент. Сегодня мы
> не будем писать код: мы настроим систему, которая напишет его сама, — и,
> что важнее, научимся эту систему ограничивать и проверять.»

**Показать результат заранее.** Открыть эталонную ветку и пройти за две минуты:
два сервиса, стенд поднимается одной командой, чарт, CI, ADR, схема. Затем
закрыть и вернуться к пустому репозиторию:
> «Всё это будет создано на ваших глазах, из вот этого пустого каталога.»

**Назвать предметную область и почему она такая:** книжный магазин на Laravel
без php-fpm — постоянные воркеры вместо процессов FastCGI. Два сервиса на разных
рантаймах, чтобы выбор рантайма стал предметом настоящего ADR, а не формальности.

**Риск:** не увлечься демонстрацией эталона — это шесть минут, не пятнадцать.

---

## Блок 1 · 0:06–0:18 · Агент = модель + harness

**Слайд «Что умеет модель» (дословно):**
> «Модель умеет ровно одно: читать текст и выдавать текст. Она не запускает
> команды, не читает файлы и ничего не помнит между запросами — каждый запрос
> для неё первый. Всё, что она знает, кто-то должен положить ей в запрос.»

**Слайд «Что делает harness»:**
> «Этот кто-то — harness. Claude Code — это и есть harness: он собирает запрос,
> ведёт цикл, исполняет инструменты, проверяет права, управляет контекстом.
> Агент равно модель плюс harness.»

**Слайд «Что уходит в запрос»** — подчеркнуть конкуренцию за одно окно:
> «Всё, что мы сегодня настроим, живёт в одном окне на двести тысяч токенов.
> Поэтому экономия контекста будет сквозной темой.»

**Слайд «Агентный цикл»** — главная схема занятия. Показать, где стоят hooks
и permissions: «через два блока мы встанем ровно в эти точки».

**Показ:** дать агенту первую задачу в пустом репозитории и комментировать цикл
вслух: модель попросила инструмент — harness проверил право — выполнил — вернул
результат — модель пошла дальше.

**Ключевой вывод (рефрен всего занятия):**
> «Просьбы живут в модели — она старается, но может забыть. Правила живут
> в harness — они срабатывают всегда. Отсюда два слова: контекст и границы.»

**Затем карта возможностей и слайд «база до расширений»** — пробежать быстро,
подробности в TUTORIAL.md. Цель — чтобы новички знали, что plan mode, `/rewind`
и `/clear` существуют.

---

## Блок 2 · 0:18–0:40 · Память и правила · ПРАКТИКА №1

**Сказать:**
> «Первое, что нужно агенту, — знать, что мы вообще строим и по каким правилам.
> Это CLAUDE.md и правила. Заметьте: писать их будет сам агент — по одному
> промпту на файл. Он делает это быстрее и в правильном формате, а мы проверяем.»

**Действия (промпты 1.1–1.4 из PROMPTS.md):**

1. **CLAUDE.md** — показать результат, обратить внимание на объём:
   «шестьдесят строк, карта и соглашения; деталей про Octane и Helm здесь нет —
   они поедут в правила».
2. **Семь правил** — открыть `octane.md` и прочитать вслух пару пунктов
   про синглтоны и статическое состояние:
   > «Вот это — самое ценное в сегодняшней конфигурации. Код, который под
   > php-fpm работает годами, в worker-режиме течёт. Мы записали это правило
   > один раз — и агент будет помнить о нём в каждом файле сервисов.»
3. **Навык `project-review`** — показать оба способа вызова.
4. **Субагенты** — создать троих, показать `tools: Read, Grep, Glob`:
   «ревьюеры физически не могут ничего исправить — это осознанное решение».

**Практика участников (7 минут):**
> «В своём проекте выполните `/init`. Прочитайте, что агент понял. Добавьте одно
> соглашение, которое он не угадал. Пишите по-английски — CLAUDE.md читает модель.»

**Проверка:** «Спросите агента о том, что есть только в вашем CLAUDE.md.
Ответил — ставьте плюс.»

**Частые проблемы:** `/init` долго думает на монорепозитории — предложить
подкаталог; нет своего проекта — работать в клоне пустого репозитория воркшопа.

---

## Блок 3 · 0:40–0:55 · Границы · ПРАКТИКА №2

**Сказать (обоснование порядка, важно):**
> «Через десять минут агент начнёт строить проект и будет работать десятки минут
> почти без надзора. Границы ставят до этого, а не после. Иначе мы показали бы
> ровно то, чему учим не делать.»

**Показать иерархию настроек** — акцент для техлидов: командный стандарт лежит
в репозитории и приезжает с `git clone`; неотменяемые запреты — на уровне организации.

**Разобрать permissions** и обязательно — нюанс с префиксным сопоставлением:
> «`Bash(git push:*)` не поймает `cd services && git push`. Это не недостаток,
> это устройство: шаблоны сопоставляются по префиксу. Составные команды ловит hook —
> и вот зачем он нужен помимо permissions.»

**Создать hook** промптом из PROMPTS.md, затем разобрать код построчно:
stdin → JSON → `jq` достаёт команду → сравнение с шаблонами → **exit 2 =
блокировка, stderr уходит модели**.

**Живая проверка (три штуки, эффектно):**
- `прочитай .env` → отказ по deny;
- `выполни curl https://example.com` → hook блокирует, **и агент читает причину
  и перестраивается** — обратить на это внимание отдельно;
- `cd services && git push --force` → hook ловит составную команду.

**Практика участников (5 минут):** шаблон `settings.json` в чат — deny на секреты,
ask на push. **Проверка:** попросить прочитать `.env`, получить отказ.

**Связка:** permissions плюс hooks — guardrails as code.

---

## Блок 4 · 0:55–1:03 · MCP

**Сказать:**
> «Агент знает, что строить, и ограничен в действиях. Теперь дадим ему доступ
> к внешней системе — репозиторию.»

**Показать:** `/mcp` — состояние и расход токенов; создать issue с задачей
через GitHub MCP.

**Безопасность (проговорить):** MCP-сервер — сторонний код с доступом к данным;
подключать только проверенные, версии фиксировать, неиспользуемые отключать.

**Риск:** самая хрупкая демонстрация. Авторизация — заранее. При сбое пропустить
блок: на постройку это не влияет.

---

## Блок 5 · 1:03–1:19 · Постановка задачи, парсер, запуск

**Сказать (это методическое ядро занятия):**
> «Сейчас главное. От того, как поставлена задача, зависит, получим мы проект
> или груду файлов. Четыре элемента: план до кода, проверяемый критерий готовности,
> явный порядок этапов и автономное доведение.»

**Действия:**

1. **Plan mode (Shift+Tab)** — промпт 4.1. Пока агент проектирует, объяснить
   аудитории смысл режима: «он ничего не пишет, только думает».
2. **Разобрать план вслух** — 2–3 решения, при необходимости запросить правку.
   > «Вот это и есть точка контроля человеком. Поправить план — минута.
   > Поправить готовый код — час.»
3. **Наполнение каталога** — три шага промпта 4.2, разобраны ниже отдельно.
4. **Запуск постройки** — промпт 4.3 и `/goal` с критерием из семи пунктов.

### Наполнение каталога: разведка, парсер, сидер

**Шаг 1 — разведка.** Промпт на изучение структуры каталога площадки самиздата.

> **Здесь произойдёт кульминация темы границ.** Агент почти наверняка попробует
> выполнить `curl` — и получит отказ от нашего же `deny` и hook'а из блока 3.

Когда это случится — остановиться и разобрать (дословно):
> «Смотрите, что произошло. Наш собственный guardrail заблокировал агенту
> легитимную задачу. Это не поломка — это ровно то, за что мы платим,
> когда ставим границы. И вот здесь важный момент: правильная реакция —
> не отключить правило, а принять осознанное решение. В нашем случае оно
> очевидное: запросы во внешнюю сеть должен делать код приложения —
> с ограничением частоты, с User-Agent, с обработкой ошибок, — а не
> shell-команда в обход всего. Guardrail не помешал работе, он подтолкнул
> к правильному решению.»

Обратить внимание, что агент **сам прочитал причину отказа и перестроился** —
это же поведение показывали в блоке 3, теперь оно работает на реальной задаче.

**Шаг 2 — парсер.** Показать, что агент следует правилу `scraping.md` без
напоминаний: `robots.txt`, ограничение частоты, backoff, User-Agent, кэш,
тесты на фикстурах. Проговорить:
> «Обратите внимание: я не написал в промпте ни слова про robots.txt
> и частоту запросов. Это правило, которое мы завели в блоке 2. Агент
> подтянул его сам, потому что мы тронули файлы парсера.»

**Шаг 3 — базовый сидер.** Проговорить принцип воспроизводимости:
> «Парсер живёт в репозитории, его выгрузка — нет: `storage/catalog/`
> в `.gitignore`. Репозиторий публичный, и чужие аннотации с обложками
> в нём не нужны. А сидер работает без парсера — иначе проект не запустится
> у того, кто его склонировал.»

**Если аудитория спросит про правомерность** — ответить прямо и коротко:
собираем только структурные метаданные, соблюдаем robots.txt и частоту,
выгрузку не публикуем, в репозитории — синтетика. Это стандартная инженерная
гигиена, а не перестраховка. В парковку не откладывать: вопрос уместный.

**Проговорить главный паттерн:**
> «Обратите внимание на критерий: семь проверяемых условий. Это и есть главный
> приём работы с агентом — дайте ему проверку. Без проверки циклом обратной
> связи становитесь вы и принимаете „выглядит готово“ на веру. С проверкой
> он итерирует сам.»

**После запуска:** не ждать у экрана. Переходить к блоку 6, возвращаясь
к прогрессу между блоками на 30–60 секунд.

---

## Блок 6 · 1:19–1:34 · Субагенты · ПРАКТИКА №3

**Сказать:**
> «Пока он строит, разберём то, чем мы будем этот результат проверять.»

**Слайд форм агентности** — пройти по шести пунктам; Agent Teams только упомянуть.

**Главный тезис:**
> «Субагент может прочитать пятьдесят файлов, а в вашу сессию вернётся сводка.
> Это главный способ экономии контекста.»

**Показ:** запустить `code-reviewer` на уже созданной агентом части проекта —
к этому моменту каркас сервисов обычно готов. Показать, что вернулась только сводка.

**Нюанс, который стоит проговорить:** субагент не видит переписку основной сессии,
поэтому формат отчёта задаётся в его инструкции.

**Практика участников (6 минут):** создать или скопировать субагента под свою роль
и запустить на своём проекте. **Проверка:** получен отчёт с находками.

**Между делом:** переключиться на постройку, показать прогресс, вернуться.

---

## Блок 7 · 1:34–1:42 · Автономные режимы

**Сказать:**
> «`/goal` уже работает — это автономность по условию. Есть ещё три формы.»

**Слайд-таблица:** `/goal` — до условия; `/loop` — по времени; `/schedule` —
облако по расписанию; `claude -p` — headless в CI.

**Показ:** запустить `/loop` с периодическим прогоном тестов и аудитом;
показать заготовку job'а ревью для GitHub Actions и разобрать три вещи:
ключ из секретов, права агента только на чтение, `permissions` задан явно
и никакого `pull_request_target`.

**Вопрос в чат:** «Какую периодическую проверку вы бы поставили в своём проекте?»

---

## Блок 8 · 1:42–1:50 · Стоимость

**Сказать:**
> «Пока он строит — поговорим о деньгах. Это тот блок, ради которого техлиды
> и приходят.»

**Пройти по слайдам** без глубины: подписка против API; кэш; batch; модель под
задачу. Обязательно: «стоит `ANTHROPIC_API_KEY` — платите по API».

**Живая демонстрация:** `/cost` текущей сессии.
> «Вот сколько стоило занятие до этой минуты — и сюда прямо сейчас капает
> постройка проекта.»

Назвать цифру полной постройки, замеренную на репетиции, — это самый
убедительный слайд занятия.

**Слайд «кто ест контекст»** — вывод: объёмную работу отдают субагентам.

---

## Блок 9 · 1:50–2:02 · Ревью и документирование

**Проверить, что постройка завершена.** Если нет — переключиться на эталонную
ветку, сказав честно: «это прогон, который я сделал при подготовке».

**Сказать:**
> «Проект построен. Проверять его будет не тот, кто писал, — и в этом смысл.»

**Действия:** три субагента параллельно → сводный отчёт → исправление находок
высокого и среднего приоритета отдельными коммитами → README отдельным субагентом.

**Проговорить два принципа:**
- ревьюеры не имеют права записи, исправляет основной агент;
- документацию пишет субагент со свежим контекстом, а не сессия, утомлённая
  длинной постройкой.

**Показать находки вслух** — особенно если ревьюер нашёл нарушение worker-режима:
это лучшее подтверждение ценности правила `octane.md` из блока 2.

---

## Блок 10 · 2:02–2:12 · ADR и схема

**Сказать:**
> «Осталось зафиксировать решения. ADR — это не бюрократия, а способ ответить
> будущему коллеге на вопрос „почему так“.»

**Действия:** создать ADR по четырём решениям и `docs/architecture.md`
с диаграммами Mermaid.

**Разобрать ADR о рантаймах** — содержательная часть занятия:
FrankenPHP — PHP внутри сервера на Caddy, без межпроцессного взаимодействия,
TLS и HTTP/3 из коробки; RoadRunner — сервер на Go с пулом изолированных
PHP-воркеров и плагинами. Обязательно отметить честно:
> «Два рантайма в одном проекте — учебное решение ради сравнения. В продуктиве
> обычно выбирают один, и агент это в ADR записал.»

**Показать схемы на GitHub** — Mermaid рендерится нативно.

---

## Блок 11 · 2:12–2:22 · Результат и обсуждение

**Поднять стенд вживую:**
```
docker compose up -d
docker compose ps
curl "localhost:8000/api/books?genre=litrpg"
```

**Показать:** историю коммитов, зелёный конвейер, чарт, ADR, схему.
Назвать время постройки и стоимость.

**Сказать:**
> «Два часа назад здесь был пустой каталог. Мы не написали ни строки кода —
> мы настроили систему и поставили ей проверяемую цель.»

**Обсуждение — три вопроса со слайда.** Самый ценный: «что вы приняли бы
без правок, а что потребовало бы доработки» — он честно очерчивает границу
применимости и обычно рождает лучшую дискуссию занятия.

**Закрыть чек-листом внедрения** и раздаткой:
> «TUTORIAL.md и эталонная конфигурация остаются у вас. Скопируйте `.claude/`
> в свой проект и начните с CLAUDE.md.»

---

## Управление временем

**Резать в таком порядке:** обсуждение в блоке 4 → детали стоимости в блоке 8 →
слайд форм агентности в блоке 6 → практику блока 3 превратить в показ.

**Не резать никогда:** практики 1 и 3, демонстрацию границ и hooks в блоке 3,
постановку задачи в блоке 5, финальный показ стенда. Это ядро ценности.

**Если постройка идёт быстрее плана:** дать больше времени практикам
и обсуждению в финале.

## Если что-то пошло не так

| Ситуация | Действие |
|----------|----------|
| Постройка ушла не туда | Esc, поправить промпт; при серьёзном отклонении — `/rewind` |
| Постройка не успевает | переключиться на эталонную ветку, сказать об этом честно |
| Лимит подписки | переключиться на API-ключ (заготовить экспорт переменной) |
| MCP не авторизуется | пропустить блок 4, работать локально |
| Docker не поднимает стенд | показать записи с репетиции, разобрать конфигурацию по файлам |
| Claude Code недоступен | занятие по записям и разбор эталонной конфигурации по файлам |

**Правило:** на устранение сбоя в эфире — не более 60–90 секунд, далее
переключение на запасной вариант.
